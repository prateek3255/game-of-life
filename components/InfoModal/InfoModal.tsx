import React from 'react';
import ReactModal from 'react-modal';


const InfoModal = React.memo(({ isOpen }: { isOpen: boolean; }) => {
    return (
        <div>
            
        </div>
    );
});

export default InfoModal;